{
    "id": "list_directory",
    "title": "List Directory",
    "description": "List files and folders in a local directory relative to the current AI working directory unless an absolute path is provided.",
    "executor": "native",
    "alwaysVisible": true,
    "inputSchema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
            "path": {
                "type": "string"
            },
            "max_entries": {
                "type": "integer",
                "minimum": 1,
                "maximum": 1000
            },
            "include_hidden": {
                "type": "boolean"
            },
            "dirs_only": {
                "type": "boolean"
            }
        }
    },
    "selection": {
        "summary": "Inspect one local directory and return structured entries.",
        "tags": ["filesystem", "directory", "list", "ls", "files", "folders"],
        "whenToUse": "Use when you need to inspect the contents of one folder before choosing files to read.",
        "whenNotToUse": "Do not use when you need recursive project shape; use directory_tree instead."
    },
    "policy": {
        "riskTier": "read",
        "approvalMode": "auto",
        "network": false,
        "idempotent": true
    },
    "native": {
        "handler": "filesystem_list_directory"
    }
}
